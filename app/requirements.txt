# 1.49 is the floor for the `width=` kwarg on st.button. The Databricks Apps
# image preinstalls an older Streamlit, and a looser floor leaves it in place —
# the app then crashes on startup with "unexpected keyword argument 'width'".
streamlit>=1.49
databricks-langchain>=0.19.0
databricks-sdk>=0.79.0
langgraph>=1.1.0
langchain>=1.0.0
# Genie One MCP tools over streamable HTTP, authenticated per-request by an
# httpx.Auth wrapper around the WorkspaceClient.
langchain-mcp-adapters>=0.3.0
httpx>=0.27
